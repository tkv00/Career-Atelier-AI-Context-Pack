# Career Atelier 기여 가이드

**[English](CONTRIBUTING.md)** · 한국어

---

Career Atelier 프로젝트에 관심을 갖고 기여해 주셔서 감사합니다.

이 문서는 Rust, Vue, Atom 등 성숙한 오픈소스 커뮤니티의 기여 규약을 따릅니다. 유용한 이슈 및 Pull Request의 형태, 커밋 로그 작성법, 코드베이스 원칙을 설명합니다. 모호하거나 이해하기 어려운 부분이 있다면 이슈를 통해 편하게 질문해 주시는 것 또한 큰 기여입니다.

---

## 목차

- [행동 강령](#행동-강령)
- [개발 환경 설정](#개발-환경-설정)
- [프로젝트 구조 및 분리 원칙](#프로젝트-구조-및-분리-원칙)
- [버그 제보 방법](#버그-제보-방법)
- [기능 제안 방법](#기능-제안-방법)
- [첫 Pull Request 작성 요령](#첫-pull-request-작성-요령)
- [커밋 메시지 규칙](#커밋-메시지-규칙)
- [코드 및 주석 스타일](#코드-및-주석-스타일)
- [데이터베이스 변경 규칙](#데이터베이스-변경-규칙)
- [README 스크린샷 갱신](#readme-스크린샷-갱신)
- [보안 취약점 제보](#보안-취약점-제보)

---

## 행동 강령

동료 기여자를 존중해 주세요. 상대방이 프로젝트를 돕기 위해 참여하고 있다고 전제합니다. 괴롭힘, 인신공격, 신규 참여자를 향한 배타적인 태도는 엄격히 금지됩니다.

---

## 개발 환경 설정

Node.js 22 버전 이상과 [Supabase CLI](https://supabase.com/docs/guides/cli)가 필요합니다.

모든 기여자는 **자신만의 독립된 Supabase 프로젝트**를 연결하여 개발합니다. 공용 개발 데이터베이스가 없으므로 다른 사람의 데이터를 손상시킬 염려가 없으며, 메인테이너에게 별도의 접속 자격증명을 요청할 필요도 없습니다.

```bash
git clone https://github.com/tkv00/Career-Atelier-AI-Context-Pack.git
cd Career-Atelier-AI-Context-Pack
npm run setup
```

`npm run setup`은 로컬 환경 도구를 점검하고, 본인의 Supabase 프로젝트를 연동하며, 마이그레이션을 적용한 뒤 `web/.env.local`과 `runner/.env` 파일을 자동으로 작성합니다. 전체 설치 절차는 [docs/USER-GUIDE.md](docs/USER-GUIDE.md)를 참고하세요.

---

## 프로젝트 구조 및 분리 원칙

| 경로 | 역할 |
|---|---|
| `web/` | Vercel에 배포되는 Next.js 웹 애플리케이션. LLM을 직접 호출하지 않으며 AI 자격증명을 갖지 않습니다. |
| `runner/` | 사용자의 개인 구독 CLI를 통해 비서를 실행하는 로컬 Node.js 프로세스. 클라우드에 배포되지 않습니다. |
| `supabase/migrations/` | 순차적으로 추가되는 Append-only SQL 마이그레이션. |
| `scripts/` | 설치 마법사 및 문서화 도구. |
| `docs/` | 사용자 가이드, 하네스 엔지니어링 및 보안 정책 문서. |

이 분리 구조는 필수적인 보안 원칙입니다. **`web/`에는 AI 자격증명이 절대 포함되지 않아야 합니다.** `web/lib/env.ts`는 빌드 시 `OPENAI_API_KEY` 같은 키가 환경변수로 감지되면 즉시 빌드를 실패시킵니다. 이는 우회해야 할 장애물이 아니라 필수적인 안전장치입니다.

---

## 버그 제보 방법

이슈를 등록하기 전, 동일한 내용이 이미 보고되어 있는지 먼저 검색해 주세요.

좋은 버그 리포트는 다음 네 가지 질문에 답합니다.

1. **무엇을 하셨나요?** 요약 대신 정확한 실행 단계를 번호 매겨 적어주세요.
2. **어떤 동작을 기대하셨나요?**
3. **실제로 어떤 결과가 발생했나요?** 설명을 서술하기보다 에러 메시지 원문을 그대로 붙여넣어 주세요.
4. **실행 환경은 무엇인가요?** 운영체제, `node -v`, 실패한 영역(`web`, `runner`, `npm run setup` 등).

러너 관련 문제라면 `npm run start` 터미널 콘솔에 출력된 로그를 함께 첨부해 주세요.

**로그를 붙여넣기 전에 개인정보를 마스킹하세요.** 터미널 로그나 스크린샷에는 본인의 이메일 주소, Supabase 프로젝트 URL, 지원 중인 회사명 등이 포함될 수 있습니다. 디버깅에 불필요한 개인 식별 정보는 반드시 지운 뒤 올려주세요.

---

## 기능 제안 방법

새로운 기능뿐 아니라 UI/UX 개선, 어색한 레이아웃 수정, 기존 기능의 동작 개선 제안도 환영합니다.

해결책을 먼저 제시하기보다 겪고 있는 구체적인 문제 상황을 설명해 주세요. "자격증 테이블을 추가해 달라"는 제안보다는 "지원서를 쓸 때마다 자격증 번호를 매번 다시 찾아서 입력해야 한다"는 제안이 훨씬 더 다양한 해결책을 논의할 수 있게 합니다.

해당 기능을 직접 구현할 의향이 있는지도 함께 알려주세요. 직접 구현하셔도 좋고 아이디어만 제안하셔도 무방하며, 이는 이슈 우선순위 일정 조율에 도움이 됩니다.

---

## 첫 Pull Request 작성 요령

1. `main` 브랜치에서 새 작업 브랜치를 생성합니다.
2. 하나의 Pull Request에는 논리적으로 연결된 하나의 변경사항만 담습니다. 리팩토링과 버그 수정이 섞여 있다면 두 개의 PR로 나누어 주세요.
3. 아래의 정적 검사를 실행합니다.
4. 단순히 작성한 코드를 나열하는 데 그치지 않고, **실제로 무엇을 실행해 검증했는지** 서술해 주세요.

```bash
cd web && npx tsc --noEmit && npm run lint && npm run build
```

정적 타입 검사 통과(`typecheck passes`)만으로는 충분하지 않습니다. "공고를 등록하고, 전형 칩을 클릭하여 데이터베이스 `stage_results` 테이블에 반영됨을 확인했다"와 같은 실제 구동 확인이 필요합니다. 이 프로젝트의 버그는 정적 검사를 모두 통과했으나 런타임에서야 드러난 경우가 많았습니다.

러너 관련 코드를 수정했다면 어떤 AI CLI 프로바이더로 테스트했는지 명시해 주세요. 구독 계정이 없어 특정 프로바이더를 테스트하지 못했다면 솔직히 기록해 주시는 것이 검토에 훨씬 유리합니다.

---

## 커밋 메시지 규칙

커밋 메시지는 Git 표준에 맞춰 **영어 명령문(Imperative mood)**으로 작성합니다.

```text
Add per-stage outcome tracking to the calendar
```

"Added...", "Adds...", "adding..." 형태로 쓰지 않습니다.

제목은 72자 이내로 간결하게 작성하고, 빈 줄을 둔 뒤 본문에 변경의 **이유(Why)**를 설명합니다. 무엇이 바뀌었는지는 diff에 드러나지만, 왜 그렇게 수정했는지는 본문에 남겨두어야 향후 히스토리를 추적할 수 있습니다.

---

## 코드 및 주석 스타일

- **주변 코드 컨벤션을 준수합니다.** 본 코드베이스는 사소하지 않은 설계 결정이나 시행착오를 거쳐 학습된 내용에 대해 **한국어로 이유(Why)를 설명하는 주석**을 남깁니다. 이 원칙을 유지해 주세요.
- **주석은 동작 메커니즘이 아니라 설계 이유를 설명합니다.** 다음 줄의 코드가 무엇을 하는지 단순히 한국어로 번역해 중복 서술하지 마세요.
- **새로운 외부 라이브러리 추가를 지양합니다.** 표준 라이브러리나 이미 설치된 패키지로 해결할 수 없는 명확한 이유가 없다면 새로운 의존성을 추가하지 마세요.
- **아직 호출처가 하나뿐인 과도한 추상화를 지양합니다.**

---

## 데이터베이스 변경 규칙

마이그레이션 파일은 **Append-only**입니다. `supabase/migrations/00NN_작업명.sql` 형태로 새 파일을 추가하세요. 이미 적용된 과거 마이그레이션 파일은 절대 수정하지 않습니다.

스키마를 변경한 후에는 TypeScript 타입을 다시 생성합니다.

```bash
supabase gen types typescript --linked > web/lib/supabase/database.types.ts
```

모든 신규 테이블은 Row Level Security(RLS)를 활성화하고 소유자 정책(`owner_all`)을 반드시 추가해야 합니다 (`supabase/migrations/0003_rls_policies.sql` 참고). RLS가 없는 테이블은 클라이언트 번들에 포함되는 anon 키로 누구나 읽을 수 있는 보안 위험이 발생합니다.

---

## README 스크린샷 갱신

`docs/images/screens/`에 포함된 스크린샷은 수동 캡처가 아니라 스크립트로 생성됩니다. 화면이 바뀌는 변경사항을 반영했다면 스크린샷을 다시 생성해 주세요.

```bash
npm run shoot-docs
```

크롬 브라우저 창이 열리면 로그인합니다. 스크립트가 세션을 감지하면 문서에 필요한 화면들을 규격에 맞춰 캡처합니다. 화면에 표시되는 이메일 주소는 캡처 전 `you@example.com`으로 자동 마스킹됩니다. 커밋 전 개인정보가 포함되지 않았는지 다시 한번 확인해 주세요.

---

## 보안 취약점 제보

보안 취약점은 공개 이슈로 등록하지 마세요. GitHub의 Private Vulnerability Reporting 기능을 사용하거나 메인테이너에게 비공개로 연락해 주시기 바랍니다.

Supabase의 `anon` 키는 공개되도록 설계된 키이며, 실제 보안 경계는 Row Level Security(RLS)입니다. anon 키를 사용해 타인의 데이터를 읽을 수 있는 경로를 발견하셨다면 이는 중대한 취약점이므로 즉시 알려주시기 바랍니다.
