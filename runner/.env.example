# Career Atelier 러너 — 로컬 전용 환경변수
#
# web/.env.example과 같은 값(anon key, 공개해도 안전 — RLS가 방어선이다).
# service_role 키는 여기에도 절대 두지 않는다 (§19.2 #2).

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# 이 기기를 구분하는 이름 (runners.device_name). 비우면 hostname을 쓴다.
RUNNER_DEVICE_NAME=
